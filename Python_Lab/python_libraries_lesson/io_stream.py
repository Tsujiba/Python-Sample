"""
#############################################################################:
・io_stream
　　→インメモリにファイルに書き込んだり、読み込んだりできる
  　→ディスクにデータが残らないので、プログラム終了時にファイルはきえる
#############################################################################
"""

import io
import requests
import zipfile


f1 = io.StringIO()
#f1 = io.BytesIO()
f1.write('test io stream')
f1.seek(0)
print(f1.read())
